google.maps.Polygon.prototype.setName = function(name) {
    this.name = name;
}
google.maps.Polygon.prototype.setIso = function(iso) {
    this.iso = iso;
}
google.maps.Polygon.prototype.setCenter = function(center) {
    this.center = center;
}
google.maps.Polygon.prototype.setCurrent = function(bln) {
    this.isCurrent = bln;
}
google.maps.Polygon.prototype.setClick = function(bln) {
    this.isClick = bln;
}
